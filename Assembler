#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    FILE* ptr;
    char ch;
    ptr = fopen("test.txt", "r");
    do {
        ch = fgetc(ptr);
        printf("%c", ch);
        //idhar 
  } while (ch != EOF);
  
